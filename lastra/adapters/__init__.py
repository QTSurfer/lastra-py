"""Optional dataframe adapters.

Each module is independent — importing it requires the corresponding
optional dependency (pandas / polars / pyarrow). Installing
``lastra[pandas]`` / ``lastra[polars]`` / ``lastra[arrow]`` pulls them
in.
"""

from __future__ import annotations
